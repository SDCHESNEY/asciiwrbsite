@page "/"

<PageTitle>ASCII Site</PageTitle>

<Hero />

<section class="home-blurb">
	<p>
		Edit <code>appsettings*.json</code> or environment overrides to change the banner, tagline, and navigation shown above.
		Plain-text mode is available via <code>curl https://localhost:5001/text</code> or by setting <code>Accept: text/plain</code> on the root URL.
	</p>
	<p>
		Visit the <a href="/about">About page</a> to confirm markdown rendering and keep the profile story in sync with <code>content/about.md</code>.
	</p>
</section>
